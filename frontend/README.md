# Frontend

Frontend app for the Email Campaign Platform.

Use the root repository `README.md` for full product, architecture, deployment, and runtime documentation.

## Local Commands
```bash
npm ci
npm run dev
npm run lint
npx tsc --noEmit
npm run build
```

## Required Environment
```env
VITE_API_BASE_URL=https://your-api-domain.com
```
